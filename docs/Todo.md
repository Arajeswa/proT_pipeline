## Todo
1) Remove the function dependence on specific labels and make them as an input. The following function have this:
    - data_loader.py
    - data_trimmer.py 
2) generate a token with the current date to append to any saving file for the session, in this way, files are not overwritten

3) in the various functions, check if modules are already imported before importing them